/** Application name — used in headers, health checks, and branding. */
export const APP_NAME = 'Ahnenbaum';

/** Current application version — sourced from package.json at build time in the future. */
export const APP_VERSION = '0.0.0';
