/**
 * Search-related types shared between server and client.
 */

/** Entity types that can appear in search results. */
export type SearchEntityType = 'person' | 'place' | 'event' | 'source' | 'media';
