/**
 * Shared tree-view constants — card dimensions used by PersonCard,
 * family-graph-layout, and the tree page connection builder.
 *
 * Single source of truth to prevent magic-number duplication.
 */

export const CARD_WIDTH = 200;
export const CARD_HEIGHT = 90;
export const CARD_HALF_HEIGHT = CARD_HEIGHT / 2; // 45
